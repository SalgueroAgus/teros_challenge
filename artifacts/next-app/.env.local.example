NEXT_PUBLIC_API_URL=    # Set after AWS deploy — ALB DNS name (e.g. http://finsight-alb-xxxx.us-east-1.elb.amazonaws.com)
NEXT_PUBLIC_API_KEY=    # Must match API_KEY set in Lambda environment variables
